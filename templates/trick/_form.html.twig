{{ form_start(form) }}    
    <div class="form-group">  
        {{ form_widget(form, {'attr':{'class': 'm-3'}}) }}
    </div>  
    <button class="btn btn-secondary m-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
