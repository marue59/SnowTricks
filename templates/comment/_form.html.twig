<div class="container">
     {{ form_start(form) }} 
    <div class="d-flex flex-wrap justify-content-between">
        {{ form_widget(form.text, { attr :{class : 'form-control', placeholder: 'Laisser un commentaire'}}) }}
        {{ form_widget(form._token) }}
        <button class="btn btn-dark m-2">{{ button_label|default('Poster') }}</button>
    </div>
    {{ form_end(form) }}
</div>
